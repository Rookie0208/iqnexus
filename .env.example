# Copy to .env in this folder (gitignored). Docker Compose reads these for local dev.
AWS_KEY=your-aws-access-key-id
AWS_SECRET=your-aws-secret-access-key
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=epocho-1.2
